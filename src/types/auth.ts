export type RegistrationFieldType = {
    username: string
    email: string
    password: string
    confirmPassword: string
}

export type LoginFieldType = {
    username: string
    password: string
}
